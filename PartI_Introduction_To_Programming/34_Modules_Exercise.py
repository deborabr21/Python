#Import the built-in statistics module.Use the mean function from it to calculate and print the
#mean of the following list: [1,2,2,3].

import statistics
nums = [1,2,2,3]
print(statistics.mean(nums))
2